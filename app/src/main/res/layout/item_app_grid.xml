<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:id="@+id/iconRoot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingHorizontal="4dp"
        android:paddingTop="12dp"
        android:paddingBottom="10dp"
        android:background="@drawable/bg_icon_press"
        android:clickable="true"
        android:focusable="true">

        <!-- Icona — 54dp, scaleType centerInside per rispettare aspect ratio -->
        <ImageView android:id="@+id/appIcon"
            android:layout_width="54dp"
            android:layout_height="54dp"
            android:scaleType="centerInside"/>

        <!-- Nome — lowercase, leggero, shadow per leggibilità su wallpaper -->
        <TextView android:id="@+id/appName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="App"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:fontFamily="sans-serif-light"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="7dp"
            android:shadowColor="#99000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="5"/>
    </LinearLayout>

    <!-- Badge notifiche — rosso, bordo bianco -->
    <TextView android:id="@+id/tvBadge"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/bg_badge"
        android:textColor="#FFFFFF"
        android:textSize="9sp"
        android:fontFamily="sans-serif-bold"
        android:gravity="center"
        android:visibility="gone"/>
</FrameLayout>
